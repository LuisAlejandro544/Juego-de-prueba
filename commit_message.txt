ci: eliminar cache en compilacion de APK Debug y generar debug keystore directamente en GitHub Actions

- Modificado `build_debug_apk.yml` desactivando el cache de Gradle (`setup-java` sin cache y flags `--no-build-cache --rerun-tasks`).
- Anadido paso para generar automaticamente `debug.keystore` con `keytool` dentro del runner de GitHub Actions para el firmado del APK Debug.
